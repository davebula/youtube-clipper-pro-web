# Netlify frontend

1. Create a Git repository with this folder or drag it into Netlify.
2. Set `NEXT_PUBLIC_API_BASE_URL` to the Render service URL.
3. Deploy using `npm run build`.

For local development, copy `.env.example` to `.env.local`, set the backend URL,
then run `npm install` and `npm run dev`.
